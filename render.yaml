services:
  - type: web
    name: smart-blog-editor-backend
    env: python
    rootDirectory: smart-blog-editor/server
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
